{% extends "FOSUserBundle::layout.html.twig" %}


{% block title %}Krosmacarte - {{ extension.nom }}{% endblock %}

{% block fos_user_content %}


    <h2>{{ extension.nom }}</h2>
    <ul>
        {% for carte in extension.cartes %}
            <li>{{ carte.nom }} - {{ carte.cout }}PA - {{ carte.dieu.nom }}</li>
        {% endfor %}
    </ul>
    <a href="{{ path('creation_carte_ext', { 'idExt': extension.id }) }}">Ajouter une carte à l'extension</a>
{% endblock %}